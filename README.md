# microk8s

```bash
alias mkctl='microk8s kubectl'
```

```bash
export KUBECONFIG=~/.microk8s/config
```

enable storage:
```bash
microk8s enable storage
```

reset cluster:
```bash
microk8s reset
```
